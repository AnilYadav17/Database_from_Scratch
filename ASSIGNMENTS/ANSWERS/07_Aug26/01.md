# 26 August 2026 - Answers

### Database and Table Creation
```sql
CREATE DATABASE Aug26;
USE Aug26;

CREATE TABLE staff_update (
    staff_id INT PRIMARY KEY AUTO_INCREMENT, 
    staff_name VARCHAR(50),
    department VARCHAR(30),
    city VARCHAR(30),
    salary DECIMAL(10,2),
    experience INT,
    age INT,
    joining_date DATE,
    status VARCHAR(20)
);

INSERT INTO staff_update (staff_name, department, city, salary, experience, age, joining_date, status) 
VALUES 
    ('Aarav', 'IT', 'Indore', 48000, 3, 26, '2022-04-15', 'Active'),
    ('Bhavna', 'HR', 'Bhopal', 56000, 5, 31, '2020-08-20', 'Active'), 
    ('Chetan', 'Finance', 'Delhi', 62000, 7, 35, '2018-03-10', 'Active'), 
    ('Divya', 'IT', 'Pune', 78000, 9, 38, '2016-11-05', 'Active'), 
    ('Eshan', 'Sales', 'Indore', 51000, 4, 29, '2021-06-18', 'Inactive'), 
    ('Farah', 'HR', NULL, 68000, 6, 34, '2019-01-25', 'Active');
```

### Answers to Queries

**Q1. Aarav's salary needs to be revised to ₹52,000. Update his salary using his employee ID.**
```sql
UPDATE staff_update 
SET salary = 52000 
WHERE staff_id = 1;
```

**Q2. Bhavna has been transferred to the Finance department in Indore. Update both her department and city.**
```sql
UPDATE staff_update 
SET department = 'Finance', city = 'Indore' 
WHERE staff_id = 2;
```

**Q3. Give a ₹4,000 increment to employees whose salary is below ₹60,000, who have at least 3 years of experience, and who do not belong to Sales.**
```sql
UPDATE staff_update 
SET salary = salary + 4000 
WHERE salary < 60000 AND experience >= 3 AND department != 'Sales';
```

**Q4. Give a ₹3,000 increment to employees who either belong to IT with at least 3 years of experience or belong to HR with at least 5 years of experience.**
```sql
UPDATE staff_update 
SET salary = salary + 3000 
WHERE (department = 'IT' AND experience >= 3) OR (department = 'HR' AND experience >= 5);
```

**Q5. Give a 10% increment to employees from IT or Finance whose current salary is below ₹70,000.**
```sql
UPDATE staff_update 
SET salary = salary * 1.10 
WHERE department IN ('IT', 'Finance') AND salary < 70000;
```

**Q6. Give a ₹2,500 increment to employees who are neither from HR nor Sales and have between 5 and 8 years of experience.**
```sql
UPDATE staff_update 
SET salary = salary + 2500 
WHERE department NOT IN ('HR', 'Sales') AND experience BETWEEN 5 AND 8;
```

**Q7. Give a ₹5,000 increment to employees whose salary is between ₹50,000 and ₹70,000, excluding Finance employees.**
```sql
UPDATE staff_update 
SET salary = salary + 5000 
WHERE salary BETWEEN 50000 AND 70000 AND department != 'Finance';
```

**Q8. Give a 7% increment to employees whose names start with either A or D.**
```sql
UPDATE staff_update 
SET salary = salary * 1.07 
WHERE staff_name LIKE 'A%' OR staff_name LIKE 'D%';
```

**Q9. Farah's city is missing. Update her city to Mumbai without modifying employees whose city is already available.**
```sql
UPDATE staff_update 
SET city = 'Mumbai' 
WHERE staff_name = 'Farah' AND city IS NULL;
```

**Q10. Give a 10% increment to employees who joined before January 1, 2020 and whose salary is below ₹70,000.**
```sql
UPDATE staff_update 
SET salary = salary * 1.10 
WHERE joining_date < '2020-01-01' AND salary < 70000;
```

**Q11. For IT employees having at least 5 years of experience, increase salary by 8%, increase experience by 1 year, and change their status to Promoted.**
```sql
UPDATE staff_update 
SET salary = salary * 1.08, 
    experience = experience + 1, 
    status = 'Promoted' 
WHERE department = 'IT' AND experience >= 5;
```

**Q12. Give a ₹6,000 increment to employees having at least 5 years of experience, salary below ₹75,000, and belonging to either HR or Finance.**
```sql
UPDATE staff_update 
SET salary = salary + 6000 
WHERE experience >= 5 AND salary < 75000 AND department IN ('HR', 'Finance');
```

**Q13. Apply the following salary revisions in a single statement (IT → 12%, HR → 9%, Finance → 8%, Sales → 5%):**
```sql
UPDATE staff_update
SET salary = CASE department
    WHEN 'IT' THEN salary * 1.12
    WHEN 'HR' THEN salary * 1.09
    WHEN 'Finance' THEN salary * 1.08
    WHEN 'Sales' THEN salary * 1.05
    ELSE salary
END;
```

**Q14. Apply salary increments according to experience (8+ yrs → 15%, 5–7 yrs → 10%, 3–4 yrs → 7%, <3 yrs → 5%):**
```sql
UPDATE staff_update
SET salary = CASE
    WHEN experience >= 8 THEN salary * 1.15
    WHEN experience BETWEEN 5 AND 7 THEN salary * 1.10
    WHEN experience BETWEEN 3 AND 4 THEN salary * 1.07
    ELSE salary * 1.05
END;
```

**Q15. Apply these salary revisions in a single statement:**
```sql
UPDATE staff_update
SET salary = CASE
    WHEN department = 'IT' AND experience >= 8 THEN salary * 1.18
    WHEN department = 'IT' THEN salary * 1.10
    WHEN department = 'HR' AND experience >= 6 THEN salary * 1.12
    WHEN department = 'HR' THEN salary * 1.07
    WHEN department = 'Finance' THEN salary * 1.08
    WHEN department = 'Sales' THEN salary * 1.05
    ELSE salary
END;
```

**Q16. Update the status of every employee according to salary:**
```sql
UPDATE staff_update
SET status = CASE
    WHEN salary >= 75000 THEN 'Senior'
    WHEN salary >= 60000 THEN 'Experienced'
    WHEN salary >= 50000 THEN 'Regular'
    ELSE 'Junior'
END;
```

**Q17. Display all employees with departments arranged in this business priority: Finance → IT → HR → Sales**
```sql
SELECT * FROM staff_update
ORDER BY CASE department
    WHEN 'Finance' THEN 1
    WHEN 'IT' THEN 2
    WHEN 'HR' THEN 3
    WHEN 'Sales' THEN 4
    ELSE 5
END;
```

**Q18. Display all employees with departments arranged in order (IT → HR → Finance → Sales), and highest salary first within each department:**
```sql
SELECT * FROM staff_update
ORDER BY CASE department
    WHEN 'IT' THEN 1
    WHEN 'HR' THEN 2
    WHEN 'Finance' THEN 3
    WHEN 'Sales' THEN 4
    ELSE 5
END, salary DESC;
```

**Q19. Display employees according to experience priority: 8+ years → 5–7 years → 3–4 years → below 3 years:**
```sql
SELECT * FROM staff_update
ORDER BY CASE
    WHEN experience >= 8 THEN 1
    WHEN experience BETWEEN 5 AND 7 THEN 2
    WHEN experience BETWEEN 3 AND 4 THEN 3
    ELSE 4
END;
```

**Q20. Give a ₹5,000 increment to only one employee (IT/HR, >=3 yrs exp, salary < ₹70,000, lowest paid first):**
```sql
UPDATE staff_update
SET salary = salary + 5000
WHERE department IN ('IT', 'HR')
  AND experience >= 3
  AND salary < 70000
ORDER BY salary ASC
LIMIT 1;
```

**Q21. Give a 10% increment to only one employee (Active, >=5 yrs exp, highest paid first):**
```sql
UPDATE staff_update
SET salary = salary * 1.10
WHERE status = 'Active'
  AND experience >= 5
ORDER BY salary DESC
LIMIT 1;
```

**Q22. Give a ₹3,000 increment to only one employee (Not HR/Sales, >=3 yrs exp, lowest paid first):**
```sql
UPDATE staff_update
SET salary = salary + 3000
WHERE department NOT IN ('HR', 'Sales')
  AND experience >= 3
ORDER BY salary ASC
LIMIT 1;
```

**Q23. Change the status to Promoted for only the highest-paid IT employee:**
```sql
UPDATE staff_update
SET status = 'Promoted'
WHERE department = 'IT'
ORDER BY salary DESC
LIMIT 1;
```

**Q24. Give a 12% increment to employees who are either IT with >=5 yrs exp or Finance earning < ₹65,000 (Active only, salary < ₹80,000):**
```sql
UPDATE staff_update
SET salary = salary * 1.12
WHERE status = 'Active'
  AND salary < 80000
  AND ((department = 'IT' AND experience >= 5) OR (department = 'Finance' AND salary < 65000));
```

**Q25. Apply the tiered salary revision in one statement:**
```sql
UPDATE staff_update
SET salary = CASE
    WHEN department = 'IT' AND experience >= 8 THEN salary * 1.18
    WHEN department = 'IT' AND experience BETWEEN 5 AND 7 THEN salary * 1.12
    WHEN department = 'HR' AND experience >= 6 THEN salary * 1.10
    WHEN department = 'HR' AND experience < 6 THEN salary * 1.07
    WHEN department = 'Finance' AND salary < 65000 THEN salary * 1.10
    WHEN department = 'Finance' AND salary >= 65000 THEN salary * 1.07
    WHEN department = 'Sales' AND experience >= 4 THEN salary * 1.06
    ELSE salary * 1.03
END;
```

**Q26. Update both salary and status according to experience:**
```sql
UPDATE staff_update
SET
    salary = CASE
        WHEN experience >= 8 THEN salary * 1.15
        WHEN experience BETWEEN 5 AND 7 THEN salary * 1.10
        WHEN experience BETWEEN 3 AND 4 THEN salary * 1.07
        ELSE salary * 1.05
    END,
    status = CASE
        WHEN experience >= 8 THEN 'Promoted'
        WHEN experience BETWEEN 5 AND 7 THEN 'Eligible'
        WHEN experience BETWEEN 3 AND 4 THEN 'Regular'
        ELSE 'Junior'
    END;
```

**Q27. Change status to Promoted for Active employees in IT/HR/Finance with >=6 yrs exp and salary >= ₹60,000:**
```sql
UPDATE staff_update
SET status = 'Promoted'
WHERE experience >= 6
  AND salary >= 60000
  AND status = 'Active'
  AND department IN ('IT', 'HR', 'Finance');
```

**Q28. IT employees with >=5 yrs exp living in Pune or Delhi receive 10% increment, city changed to Bangalore, and status to Transferred:**
```sql
UPDATE staff_update
SET salary = salary * 1.10,
    city = 'Bangalore',
    status = 'Transferred'
WHERE department = 'IT'
  AND experience >= 5
  AND city IN ('Pune', 'Delhi');
```

**Q29. Give a ₹7,000 special increment to only one eligible employee (IT/HR/Finance, >=4 yrs exp, Active, salary < ₹75,000, lowest paid first):**
```sql
UPDATE staff_update
SET salary = salary + 7000
WHERE department IN ('IT', 'HR', 'Finance')
  AND experience >= 4
  AND status = 'Active'
  AND salary < 75000
ORDER BY salary ASC
LIMIT 1;
```

**Q30. Annual salary revision and status update in one statement (Inactive employees receive no increment):**
```sql
UPDATE staff_update
SET
    salary = CASE
        WHEN status = 'Inactive' THEN salary
        WHEN department = 'IT' AND experience >= 8 AND salary >= 70000 THEN salary * 1.18
        WHEN department = 'IT' AND experience >= 5 THEN salary * 1.12
        WHEN department = 'HR' AND experience >= 6 THEN salary * 1.10
        WHEN department = 'HR' AND experience < 6 THEN salary * 1.07
        WHEN department = 'Finance' AND salary < 65000 THEN salary * 1.10
        WHEN department = 'Finance' AND salary >= 65000 THEN salary * 1.07
        WHEN department = 'Sales' AND experience >= 4 THEN salary * 1.06
        ELSE salary * 1.03
    END,
    status = CASE
        WHEN status = 'Inactive' THEN status
        WHEN (department = 'IT' AND experience >= 8 AND salary >= 70000) THEN 'Promoted'
        WHEN (department = 'IT' AND experience >= 5)
          OR (department = 'HR' AND experience >= 6)
          OR (department = 'Finance' AND salary < 65000) THEN 'Eligible'
        ELSE 'Under Review'
    END
WHERE status != 'Inactive';
```
